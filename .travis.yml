language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "nightly"
# command to install dependencies
install:
    - "pip install -e .[test]"
# command to run tests
script: nosetests --exclude-dir=/datasketch/test/aio
