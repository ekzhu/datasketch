language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "nightly"
# command to install dependencies
install:
    - "pip install numpy-1.11.0"
    - "pip install ."
# command to run tests
script: nosetests
